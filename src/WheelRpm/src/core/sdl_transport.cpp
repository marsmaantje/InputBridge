// sdl_transport.cpp
// SDLTransport is defined in wheel/sdl_transport.hpp so it can be shared
// across translation units without re-definition.  This .cpp file exists only
// so CMake can verify the header compiles cleanly in isolation.
#include "wheel/sdl_transport.hpp"
