#pragma once

/**
 * @brief Draws the Debug Log content panel.
 *
 * Reads from AppLog and renders a scrollable, filterable log viewer.
 * Call once per frame when the Debug Log section is active.
 */
void DrawDebugLogContent();
